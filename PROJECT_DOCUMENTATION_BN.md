# 🚦 স্মার্ট ট্রাফিক সিগন্যাল সিমুলেশন (Adaptive Traffic Signal Simulation)
## সম্পূর্ণ প্রজেক্ট পরিচিতি ও কোড আর্কিটেকচার ডকুমেন্টেশন (বাংলায়)

---

## ১. প্রজেক্টের সংক্ষিপ্ত বিবরণ (Project Overview)
এই প্রজেক্টটি **C++** এবং **WinBGIm (C++ Graphics Library)** ব্যবহার করে তৈরি একটি **2D স্মার্ট ট্রাফিক সিগন্যাল কন্ট্রোল সিমুলেশন**।

ঐতিহ্যবাহী ট্রাফিক সিগন্যাল সাধারণত নির্দিষ্ট সময়ের ফিক্সড টাইমার (Fixed Timer) মেনে চলে, যা বাস্তব জীবনের ট্রাফিক জ্যাম কমাতে পারে না। এই প্রজেক্টে একটি **স্মার্ট অ্যাডাপ্টিভ অ্যালগরিদম (Adaptive Algorithm)** যুক্ত করা হয়েছে, যা রোডের রিয়েল-টাইম গাড়ির ঘনত্ব (Vehicle Density) মেপে ডায়নামিকভাবে সিগন্যালের সবুজ বাতির সময়সীমা (Green Light Duration) নিয়ন্ত্রণ করে।

এছাড়াও এতে রয়েছে **৭ ধরণের বৈচিত্র্যময় যানবাহন (Car, Bus, Bike, SUV, Truck, Taxi, Ambulance)**, রিয়েলিস্টিক ফিজিক্স ও কলিশন অ্যাভয়ডেন্স, এবং অপারেটিং সিস্টেমের অন্যতম প্রধান কনসেপ্ট **Deadlock (Gridlock) Detection & Resolution System**।

---

## ২. প্রজেক্টের ফাইল স্ট্রাকচার ও কার্যাবলী (File by File Breakdown)

প্রজেক্টটি অবজেক্ট ওরিয়েন্টেড প্রোগ্রামিং (OOP) আর্কিটেকচারে সুবিন্যস্তভাবে সাজানো হয়েছে:

| ফাইলের নাম | ফাইলের ধরণ | মূল দায়িত্ব ও ভূমিকা |
| :--- | :--- | :--- |
| `main.cpp` | Driver / Entry Point | মেইন গেম লুপ, ইউজার ইনপুট (কিবোর্ড ও মাউস), ডাবল বাফারিং ও FPS কন্ট্রোল। |
| `Config.h` | Global Header | উইন্ডোর মাপ, রাস্তার জ্যামিতি, স্টপ লাইন, টাইমিং কনস্ট্যান্ট ও সব Enum সংজ্ঞা। |
| `Vehicle.h` / `Vehicle.cpp` | Model & 2D Graphics | যানবাহনের ক্লাস, ৭ ধরণের গাড়ির গ্রাফিক্স রেন্ডারিং, গতি, অ্যাক্সিলারেশন ও বাম্পার হিসাব। |
| `TrafficLight.h` / `TrafficLight.cpp` | State Machine Controller | ৪-ফেজ ট্রাফিক লাইট স্টেইট মেশিন এবং অ্যাডাপ্টিভ গ্রিন টাইম ক্যালকুলেটর। |
| `Intersection.h` / `Intersection.cpp` | Core Simulation Manager | সব গাড়ি ম্যানেজ করা, কলিশন ডিস্ট্যান্স, ডেডলক ডিটেকশন, প্রিভেনশন ও রেজোলিউশন। |
| `HUD.h` / `HUD.cpp` | User Interface (UI) | রাস্তা ও সিনারি, জেব্রা ক্রসিং, লাইট বক্স, ডেডলক অ্যালার্ট ও সাইডবার ড্যাশবোর্ড ড্রয়িং। |

---

## ৩. প্রতিটি ফাইলের বিস্তারিত ব্যাখ্যা (Detailed Component Analysis)

### ১) `Config.h` — গ্লোবাল কনফিগারেশন
সব কনস্ট্যান্ট ও গ্লোবাল সেটিংস এই ফাইলে সংরক্ষিত:
- **স্ক্রিন সাইজ**: `SCREEN_WIDTH = 1000`, `SCREEN_HEIGHT = 750`, ড্যাশবোর্ড শুরু `HUD_X_START = 710`।
- **রাস্তার কোঅর্ডিনেট**: সেন্ট্রাল ইন্টারসেকশন `(355, 375)`, রাস্তার প্রস্থ `160px` (প্রতিটি লেন `80px`)।
- **স্টপ লাইন ও ডিটেকশন জোন**: স্টপ লাইনের পেছনে `180px` দৈর্ঘ্য ডিটেকশন জোন হিসেবে চিহ্নিত।
- **টাইমিং ও ডেডলক কনস্ট্যান্ট**: 
  - `BASE_GREEN_TIME = 6.0s` (বেস গ্রিন টাইম)
  - `TIME_PER_CAR = 1.5s` (প্রতিটি অতিরিক্ত গাড়ির জন্য বাড়তি সময়)
  - `MAX_GREEN_TIME = 20.0s`, `MIN_GREEN_TIME = 4.0s`
  - `DEADLOCK_WAIT_THRESHOLD = 2.5s`
- **Enums**:
  - `Direction`: `DIR_SOUTH`, `DIR_NORTH`, `DIR_EAST`, `DIR_WEST`
  - `SignalPhase`: `PHASE_NS_GREEN`, `PHASE_NS_YELLOW`, `PHASE_EW_GREEN`, `PHASE_EW_YELLOW`
  - `ControlMode`: `MODE_ADAPTIVE`, `MODE_FIXED`
  - `VehicleType`: `VEHICLE_SEDAN`, `VEHICLE_SUV`, `VEHICLE_BUS`, `VEHICLE_BIKE`, `VEHICLE_TRUCK`, `VEHICLE_TAXI`, `VEHICLE_AMBULANCE`

---

### ২) `Vehicle.h` ও `Vehicle.cpp` — যানবাহন মডেল ও গ্রাফিক্স
প্রতিটি গাড়ির লাইফসাইকেল ও ভিজ্যুয়াল রূপ এখানে সংজ্ঞায়িত:

#### ক) ৭ ধরণের ভেহিকল আর্কিটেকচার:
1. **Sedan (Car)** (দৈর্ঘ্য ৩৬px, প্রস্থ ২০px): স্ট্যান্ডার্ড প্রাইভেট কার, অ্যারোডাইনামিক বডি, টিন্টেড উইন্ডশীল্ড, হেডলাইট ও টেইললাইট।
2. **SUV / Jeep** (দৈর্ঘ্য ৪০px, প্রস্থ ২২px): বড় ফ্রেম, সিলভার রুফ লাগেজ র‍্যাক, পেছনের স্পেয়ার টায়ার।
3. **Transit Bus** (দৈর্ঘ্য ৬২px, প্রস্থ ২৪px): লম্বা সিটি বাস, দুইপাশে প্যাসেঞ্জার উইন্ডো সারি, রুফ এসি ইউনিট, ডেস্টিনেশন সাইনবোর্ড।
4. **Motorbike** (দৈর্ঘ্য ২২px, প্রস্থ ১২px): স্লিম বডি, ফ্রন্ট-রিয়ার টায়ার, হ্যান্ডেলবার এবং হেলমেট পরিহিত রাইডার।
5. **Delivery Truck** (দৈর্ঘ্য ৫০px, প্রস্থ ২৩px): ড্রাইভার কেবিন এবং পেছনে বড় কার্গো বক্স কন্টেইনার।
6. **Yellow Taxi** (দৈর্ঘ্য ৩৬px, প্রস্থ ২০px): আইকনিক হলুদ ট্যাক্সি, রুফে লাইটিং **"TAXI"** সাইন এবং চেকার্ড স্ট্রাইপ।
7. **Ambulance** (দৈর্ঘ্য ৪২px, প্রস্থ ২২px): হাসপাতালের সাদা বডি, লাল সাইড স্ট্রাইপ, রুফে রেড ক্রস (+) এবং লাল-নীল ইমার্জেন্সি বীকন।

#### খ) ফিজিক্স ও মুভমেন্ট লজিক:
- `update()`: সামনের গাড়ির দূরত্ব (`distToAhead`) এবং স্টপ লাইনের দূরত্ব (`distToStopLine`) দেখে গতি বাড়ায় বা কমায়।
- `getFrontBumperPos()` এবং `getRearBumperPos()`: গাড়ির সাইজ অনুযায়ী সামনের ও পেছনের বাম্পারের সঠিক অবস্থান বের করে।
- `isInIntersectionBox()`: গাড়িটি ইন্টারসেকশন মোড়ের ভেতরের বক্সে আছে কিনা তা যাচাই করে।

---

### ৩) `TrafficLight.h` ও `TrafficLight.cpp` — ট্রাফিক সিগন্যাল কন্ট্রোলার
সিগন্যালের ফেজ পরিবর্তন ও টাইমিং হিসাবের দায়িত্বে নিয়োজিত:

#### ক) ৪-ফেজ সাইকেল (Signal Phase Cycle):
1. `PHASE_NS_GREEN`: North-South সবুজ, East-West লাল।
2. `PHASE_NS_YELLOW`: North-South হলুদ (৩ সেকেন্ড), East-West লাল।
3. `PHASE_EW_GREEN`: East-West সবুজ, North-South লাল।
4. `PHASE_EW_YELLOW`: East-West হলুদ (৩ সেকেন্ড), North-South লাল।

#### খ) অ্যাডাপ্টিভ গ্রিন টাইম ফর্মুলা:
$$\text{Green Time} = \text{Clamp}\Big(\text{BASE\_GREEN\_TIME} + (\text{Waiting Cars} \times \text{TIME\_PER\_CAR}),\; \text{MIN\_GREEN},\; \text{MAX\_GREEN}\Big)$$
- লেনে গাড়ি বেশি থাকলে স্বয়ংক্রিয়ভাবে সবুজ বাতির সময় বেড়ে যায় (সর্বোচ্চ ২০ সেকেন্ড পর্যন্ত)।
- লেনে গাড়ি কম থাকলে সবুজ বাতি দ্রুত হলুদ হয়ে অন্য লেনকে সুযোগ দেয়।

---

### ৪) `Intersection.h` ও `Intersection.cpp` — ইন্টারসেকশন ম্যানেজার ও ডেডলক সিস্টেম
এটি পুরো সিমুলেশনের সেন্ট্রাল ব্রেন:

#### ক) ট্রাফিক ম্যানেজমেন্ট:
- `spawnVehicle()`: নির্ধারিত লেনে নতুন গাড়ি স্পন করে (সামনে পর্যাপ্ত ফাঁকা জায়গা আছে কিনা চেক করে)।
- `calculateDistanceToAhead()`: একই লেনের সামনের গাড়ি থেকে পেছনের গাড়ির সঠিক বাম্পার-টু-বাম্পার দূরত্ব নির্ণয় করে।

#### খ) ডেডলক ডিটেকশন ও রেজোলিউশন (Deadlock System):
1. **Deadlock Detection (`checkDeadlock`)**:
   - ইন্টারসেকশন বক্সের ভেতর বিভিন্ন দিক থেকে আসা গাড়ি `2.5s` এর বেশি সময় স্থির হয়ে থাকলে সিস্টেম **`DEADLOCK DETECTED`** ঘোষণা করে।
   - ডেডলকে আটকে থাকা গাড়ির চারপাশে লাল-হলুদ ভিজ্যুয়াল হাইলাইট আঁকা হয়।
2. **Anti-Deadlock Guard (`isBoxExitClear` - "Don't Block The Box")**:
   - সবুজ বাতি থাকলেও গাড়ি দেখে যে মোড় পার হয়ে ওপাশের লেনে যাওয়ার মতো জায়গা আছে কিনা। যদি ওপাশ ব্লক থাকে, গাড়ি স্টপ লাইনেই দাঁড়িয়ে থাকে—ফলে ক্রস ট্রাফিকের রাস্তা বন্ধ হয় না।
3. **Deadlock Resolution (`resolveDeadlock`)**:
   - ডেডলক হলে স্বয়ংক্রিয়ভাবে অথবা `[R]` চাপলে একটি **Priority Green Wave** শুরু হয়। জ্যামে আটকে থাকা লেনকে গ্রিন ফেজ দিয়ে অন্য লেনকে থামিয়ে ইন্টারসেকশনটি ক্লিয়ার করে দেওয়া হয়।
4. **Force Deadlock Demo (`forceDeadlockScenario`)**:
   - `[D]` চাপলে ৪ দিক থেকে একসাথে গাড়ি মোড়ের মধ্যে ঢুকিয়ে ৪-মুখী ডেডলক বা গ্রিডলকের জীবন্ত ডেমো তৈরি করে।

---

### ৫) `HUD.h` ও `HUD.cpp` — গ্রাফিক্স ও ড্যাশবোর্ড UI
ইউজারের দেখার জন্য সব ভিজ্যুয়াল এলিমেন্ট তৈরি করে:
- **রোড সিনারি**: ডার্ক স্লেট পিচ ঢালাই রাস্তা, ডিভাইডারে ডাবল হলুদ দাগ, সাদা জেব্রা ক্রসিং।
- **ডিটেকশন জোন**: ৪টি অ্যাপ্রোচে উজ্জ্বল সায়ান কালারের ফ্রেম।
- **ট্রাফিক লাইট বক্স**: সিগন্যাল পোস্টের ভেতরে জ্বলন্ত লাল, হলুদ, ও সবুজ বাল্ব।
- **ড্যাশবোর্ড প্যানেল**:
  - সিস্টেম মোড ব্যাজ (`[ MODE: ADAPTIVE SMART ]` / `[ FIXED TIMER ]`)
  - ডেডলক স্ট্যাটাস ব্যানার (`TRAFFIC FLOWING` / `DEADLOCK DETECTED` / `RESOLVING WAVE`)
  - লাইভ সিগন্যাল কাউন্টডাউন টাইমার ও প্রগ্রেস বার
  - প্রতি লেনের লাইভ গাড়ির সংখ্যা ও ডেনসিটি গ্রাফ বার
  - পারফরম্যান্স মেট্রিক্স (মোট ক্লিয়ার হওয়া গাড়ি, গড় অপেক্ষার সময়, ডেডলক কাউন্টার)
  - ইন্টারেক্টিভ কিবোর্ড কন্ট্রোলস নির্দেশিকা

---

### ৬) `main.cpp` — মেইন লুপ ও ইভেন্ট হ্যান্ডলিং
- উইন্ডো তৈরি (`1000 x 750`)।
- **কিবোর্ড ও মাউস ইভেন্ট লিসেনার**:
  - `[N / S / E / W]`: নির্দিষ্ট লেনে গাড়ি স্পন করা।
  - `[Mouse Click]`: রাস্তার যে লেনে ক্লিক করা হবে সেখানে গাড়ি স্পন হবে।
  - `[D]`: ৪-মুখী ডেডলক তৈরি করা।
  - `[R]`: ডেডলক সমাধান করা।
  - `[X]`: Anti-Deadlock "Don't Block the Box" গার্ড অন/অফ করা।
  - `[M]`: Adaptive ও Fixed মোডের মধ্যে পরিবর্তন।
  - `[B]`: Burst Traffic Spike (একসাথে ৪ লেনে গাড়ি স্পন)।
  - `[C]`: সব গাড়ি ক্লিয়ার করা।
  - `[P]`: সিমুলেশন পজ / রিজ্যুম করা।
  - `[ESC]`: সিমুলেশন বন্ধ করা।
- **ডাবল বাফারিং (Double Buffering)**: `setactivepage()` এবং `setvisualpage()` ব্যবহার করে ফ্লিকারিং মুক্ত মসৃণ ৬০ FPS রেন্ডারিং নিশ্চিত করা হয়েছে।

---

## ৪. কীভাবে প্রজেক্টটি কম্পাইল ও রান করবেন (Build & Run Guide)

### টার্মিনালে কম্পাইল করার কমান্ড:
```powershell
g++.exe -std=c++17 -fdiagnostics-color=always -g main.cpp Vehicle.cpp TrafficLight.cpp Intersection.cpp HUD.cpp -o traffic_sim.exe -lbgi -lgdi32 -lcomdlg32 -luuid -loleaut32 -lole32
```

### রান করার কমান্ড:
```powershell
.\traffic_sim.exe
```

---

## ৫. সংক্ষেপে অ্যালগরিদম ও ফিচার সারসংক্ষেপ (Summary)
1. **Adaptive Green Timing**: ট্রাফিক জ্যামের গাড়ি গুনে স্বয়ংক্রিয়ভাবে সিগন্যালের সময় বাড়ানো-কমানো।
2. **Safe Following & Collision Avoidance**: সামনের গাড়ির পেছনের বাম্পার থেকে নিরাপদ দূরত্বে পেছনের গাড়ি থেমে থাকা।
3. **Vehicle Diversity**: ৭ ধরণের ভিন্ন সাইজ, গতি ও আকৃতির বাস্তবসম্মত যানবাহন।
4. **Deadlock Management**: ৪-মুখী জটলা স্বয়ংক্রিয়ভাবে শনাক্ত করা, বক্স ব্লকিং প্রতিরোধ করা এবং প্রায়োরিটি ওয়েভের মাধ্যমে ডেডলক মুক্ত করা।
